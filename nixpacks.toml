# Nixpacks config for Railway deploy (INFRA task 4)
# Repo root has no requirements.txt — deps live in backend/requirements.txt.
[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8765}"
